# Pin pnpm settings that the committed lockfile was generated with, so `pnpm install
# --frozen-lockfile` is reproducible in CI (a fresh runner defaults auto-install-peers to
# true, which mismatches the lockfile's `settings.autoInstallPeers: false` and fails the job).
auto-install-peers=false
